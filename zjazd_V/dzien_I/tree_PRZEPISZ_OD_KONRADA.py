#len(list(path.iterdir())) zeby zliczyc path (ścieżki) musze stworzyć listę, przy liście można policzyc ścieżki- użyć len;
#bez stworzenia listy nie bede mogła uzyć len

#prefix- (nazwa zmiennej?)
#marker- (nazwa zmiennej?)
#next_marker- (nazwa zmiennej?)

#Konrad zrobił plik "tree" w folderze "file" ("importowanie_i_odczytywanie_plikow"->"file"->"tree")

# suffix -> https://docs.python.org/3/library/pathlib.html